// import Atropos from 'atropos';
// import 'atropos/css'

// ALTROPOS
// top page
const myAtropos = Atropos({
  el: ".my-atropos",
  rotate: true,
  // rotateTouch: true,
  alwaysActive: false,
  rotateXMax: 1.5,
  rotateYMax: 1.5,
  activeOffset: 15,
  highlight: false,
  // shadow: true,
  // shadowOffset: 50,
  // shadowScale: 1,
  // durationEnter: 1000,
});

// google map

const myAtroposMap = Atropos({
  el: ".my-atropos-map",
  rotate: true,
  // rotateTouch: true,
  alwaysActive: false,
  rotateXMax: 1.5,
  rotateYMax: 1.5,
  activeOffset: 15,
  highlight: false,
  // shadow: true,
  // shadowOffset: 50,
  // shadowScale: 1,
  // durationEnter: 1000,
});
